How to Read a File and Convert To String (When Testing)
-------------------------------------------------------

Procedure
---------
 1. Add a dependency for commons-io
 
     <dependency>
    	 <groupId>org.apache.commons</groupId>
    	 <artifactId>commons-io</artifactId>
    	 <version>1.3.2</version>
    </dependency>


 2. Place the text file you want your test class to use in /src/test/resources/newFile.txt
 
 3. Add this code
      InputStream is = MyClass.class.getReosurceAsStream("/newFile.txt");
      
      // Convert the input stream into a string
      String sFileAsString = IOUtils.toString(is;)
      
      // Then, do something with your string (like if the string holds JSON, then conver it to an instance of a class
      SomeModelClass mySomeModelClass = GSON.fromJosn(sJson, SomeModelClass.class);
