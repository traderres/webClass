Lesson:  Parse the XML in a Test Class
--------------------------------------

Assumptions:
 A) You want to test your XML parsing in a test class
 
 
Procedure
---------
 1. Add dependencies to your pom.xml
 
    <dependency>
       <groupId>commons-io</groupId>
       <artifactId>commons-io</artifactId>
       <version>2.4</version>
       <scope>test</scope>
    </dependency> 
    
    <dependency>
       <groupId>com.sun.xml.bind</groupId>
       <artifactId>jaxb-impl</artifactId>
       <version>2.2.5</version>
       <scope>test</scope>
    </dependency>    
    
    
 2. Create this file:  sample1.xml
    Right-click on /src/test/resources -> new file
    filename:  sample1.xml
    
    
 3. Create a test class
    Right-click on /src/test/java
    
    public Class XmlParsingTest
    {
    
       @Test
       public void testXmlParsing()
       {
           JAXBContext jaxbContext = JAXBContext.newInstance( TargetClass.class);
           
           InputStream is = XmlParsingTest.class.getReousrceAsStream("/sample1.xml");
           
           // Read the file into a string
           String sXml = IOUtils.toSTring(is);
           
           StringReader stringReader = new StringReader(sXml);
           
           // Convert XML into java object
           TargetClass targetClass = (TargetClass) jaxbContext.createUnmarshaller().unmarshal(stringReader);
           
           Assert....
           
       }
    }
    
