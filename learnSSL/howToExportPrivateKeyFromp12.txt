How to Export Private Key from P12
----------------------------------

Procedure
---------
unix> cd /tmp
unix> openssl pkcs12 -in sample.p12  -nodes -nocerts -out sample.key
      Enter import password:
      -- Generates /tmp/sample.key


