Problem:  When I do a git push, IntelliJ prompts me for the Gitlab Account
          But, I used git clone with an access token so the token is there

Solution: Check "Use Credential Helper"
		  Pull File -> Settings -> Version Control -> Git
		  Check "Use credential helper"

		  Now, intellij will use the access token from the command line

