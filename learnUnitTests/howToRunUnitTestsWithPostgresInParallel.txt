How to Run Unit Tests with Postgres in Parallel
-----------------------------------------------
Problem:  I have a 100 unit tests to run and I want to run in parallel
          But, each unit test makes changes

Solution: Wrap each unit test in a transaction and do *NOT* commit the transaction
          When the unit test finishes, the database is unchanged

